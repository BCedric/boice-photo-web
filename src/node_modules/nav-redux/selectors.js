const galleriesSelector = ({navState}) => {
  return navState.galleries
}

const galleriesListsSelector = ({navState}) => {
  return navState.galleriesLists
}

const currentGalleriesSelector = ({navState}) => {
  return navState.currentGalleries
}

const galleriesNotInListsSelector = ({navState}) => {
  return navState.galleriesNotInLists
}

const currentGallerieSelector = state => state.galleryState

export { galleriesSelector, galleriesListsSelector, currentGalleriesSelector, currentGallerieSelector, galleriesNotInListsSelector}
