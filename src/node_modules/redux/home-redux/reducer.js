import { types } from './actions'

export default function homeReducer(state = {}, action) {
    switch (action.type) {
        case types.SET_CAROUSEL_GALLERIES:
            return { ...state, carouselGalleries: action.galleries }
        default:
            return state
    }
}