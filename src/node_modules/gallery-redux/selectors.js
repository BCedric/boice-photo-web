// import { createSelector } from 'reselect'

const imgAddrSelector = state => state.imgAddr
const imgSelector = state => state.imgs
const galleryIdSelector = state => state.galleryId
const nbImgsSelector = state => state.nbImgs
const currentImageSelector = state => state.currentImage
const lightboxIsOpenSelector = state => state.lightboxIsOpen

export { imgAddrSelector, imgSelector, galleryIdSelector, nbImgsSelector, currentImageSelector, lightboxIsOpenSelector }
