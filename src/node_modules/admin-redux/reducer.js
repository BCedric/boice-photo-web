import {types} from './actions'

export default function adminReducer(state = {}, action) {
  switch(action.type) {
    case types.SET_MESSAGE:
      return {...state, message: action.message}
    case types.SAVE_ADRESS_SERVER:
      return {...state, adressServer: action.adress}
    default:
      return state
  }
}
