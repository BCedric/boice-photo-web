import React from 'react'

import './fade-component.css'

const FadeComponent = ({ display, children }) =>
  (<div className={display ? 'show' : 'hidden'} >
    {children}
  </div>)


export default FadeComponent
